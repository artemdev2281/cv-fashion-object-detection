"""Модель SSD (Single Shot MultiBox Detector, одноэтапный детектор).

Модуль инкапсулирует создание, обучение и инференс модели SSD, выполняющей
предсказание рамок на многомасштабных картах признаков с использованием
default-боксов. Интерфейс согласован с остальными детекторами проекта.
Содержательная реализация добавляется на этапе обучения моделей
(разделы 3.5–3.6 отчёта).
"""

from __future__ import annotations


def build_model(num_classes: int, config: dict | None = None):
    """Создать модель SSD для заданного числа классов.

    Параметры
    ---------
    num_classes:
        Число категорий одежды (учитывая фоновый класс при необходимости).
    config:
        Параметры модели и обучения (см. ``configs/default.yaml``).
    """
    raise NotImplementedError("Реализация SSD будет добавлена на этапе обучения.")
