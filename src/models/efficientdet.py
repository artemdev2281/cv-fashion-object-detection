"""Модель EfficientDet (детектор, оптимизированный по эффективности).

Модуль инкапсулирует создание, обучение и инференс модели EfficientDet,
использующей сеть-основу EfficientNet, взвешенную двунаправленную пирамиду
признаков (BiFPN) и согласованное масштабирование. Интерфейс согласован с
остальными детекторами проекта. Содержательная реализация добавляется на
этапе обучения моделей (разделы 3.5–3.6 отчёта).
"""

from __future__ import annotations


def build_model(num_classes: int, config: dict | None = None):
    """Создать модель EfficientDet для заданного числа классов.

    Параметры
    ---------
    num_classes:
        Число категорий одежды.
    config:
        Параметры модели и обучения (см. ``configs/default.yaml``).
    """
    raise NotImplementedError(
        "Реализация EfficientDet будет добавлена на этапе обучения."
    )
