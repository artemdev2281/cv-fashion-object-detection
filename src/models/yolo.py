"""Модель YOLOv8 (одноэтапный детектор семейства YOLO).

Модуль инкапсулирует создание, обучение и инференс модели YOLOv8 на базе
библиотеки Ultralytics в едином интерфейсе, согласованном с остальными
детекторами проекта. Содержательная реализация добавляется на этапе обучения
моделей (разделы 3.5–3.6 отчёта).
"""

from __future__ import annotations


def build_model(num_classes: int, config: dict | None = None):
    """Создать модель YOLOv8 для заданного числа классов.

    Параметры
    ---------
    num_classes:
        Число категорий одежды.
    config:
        Параметры модели и обучения (см. ``configs/default.yaml``).
    """
    raise NotImplementedError("Реализация YOLOv8 будет добавлена на этапе обучения.")
